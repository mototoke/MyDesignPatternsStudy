version: "3"
services:
  code:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    environment:
      PASSWORD: password # change your password
    ports:
      - 53000:8080 # code-server
    volumes:
      - ./src:/home/coder/project
      - ./code-server:/home/coder/.local/share/code-server

# volumes:    
#   postgres:
#     driver: local